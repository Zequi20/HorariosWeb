class Usuario {
  Usuario(this.id, this.nombre);
  final int id;
  final String nombre;
}

class User {
  User(this.id, this.nombre, this.clave);
  final int id;
  final String nombre;
  final String clave;
}
